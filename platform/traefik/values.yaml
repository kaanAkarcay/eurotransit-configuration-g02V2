providers:
  kubernetesIngress:
    enabled: true
    ingressClass: traefik

service:
  type: LoadBalancer
  annotations:
    service.beta.kubernetes.io/azure-dns-label-name: g02-entrypoint

log:
  level: DEBUG

accessLog:
  enabled: true
